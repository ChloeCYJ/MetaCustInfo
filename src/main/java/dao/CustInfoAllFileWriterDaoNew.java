package dao;

import java.sql.Connection;

/** 기존 개인정보 제공 배치 DAO(기존 시스템 연계 지점). */
public class CustInfoAllFileWriterDaoNew {
    public void execute(Connection conn) {
        // 실제 기존 배치 로직은 내부 반입 환경의 기존 소스를 사용.
    }
}
